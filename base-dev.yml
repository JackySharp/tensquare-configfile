server:
  port: 9021
spring:
  application:
    name: tensquare-base
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.25.130:3306/tensquare_base?characterEncoding=utf8&useSSL=false
    password: root
    username: root
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
eureka:
  client:
    service-url:
      defauleZone: http://localhost:6868/eureka
  instance:
    prefer-ip-address: true